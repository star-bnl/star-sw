<job  name="TpcRS" maxFilesPerProcess="1"  filesPerHour="1" simulateSubmission="false" fileListSyntax="paths">
<stdout URL="file:/star/u/fisyak/prodlog/$JOBID.log" />
<stderr URL="file:/star/u/fisyak/prodlog/$JOBID.err" />
<command> 
  source ~/.tcshrc
  cd ${SUBMITTINGDIRECTORY}  
#  starver .DEV2 
#  setup gcc451
  set evtsFile = $INPUTFILE0;
  set file     = `basename $evtsFile .MuDst`
  echo "${evtsFile}"
  if (! -r ${evtsFile}) exit 1;
  set logFile  = ${file}B.log
  set f=`find . -maxdepth 1 -type f -name "${file}*.root"`
  if ("$f" == "") then
    root4star -q -b 'TpcRS.C(4000,"y2005,TpcRS","'${evtsFile}'","Bichsel,PhysicsOff")' &gt;&amp; ${logFile}
  endif
</command>
<input URL="file:/star/subsys/tpc/fisyak/Tpc/TpcRS/daq_2005_CuCu22/*MuDst.root" /> 
</job>
