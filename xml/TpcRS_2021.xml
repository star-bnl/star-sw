<?xml version="1.0" encoding="utf-8" ?>

<job  name="TpcRS" maxFilesPerProcess="1"  filesPerHour="1" simulateSubmission="false" fileListSyntax="paths" copyInputLocally="false">
<stdout URL="file:./sched$JOBID.log" />
<stderr URL="file:./sched$JOBID.err" />
<command> 
  cd ${SUBMITTINGDIRECTORY}  
setenv STARFPE NO
#  unsetenv DEBUG yes
#   setup debug
#  setup 64b
#  starver .DEV2 
#  starver TFG17j
#   starver TFG19e
#  starver .DEV2
  limit stacksize 16m
  echo $INPUTFILE0 | grep ':'
  if (! $?) then
    set evtsFile = `echo $INPUTFILE0 | awk -F\: '{print $1}'`
    set f        = `echo $INPUTFILE0 | awk -F\: '{print $2}'`
    set l        = `echo $INPUTFILE0 | awk -F\: '{print $3}'`
    set file     = `echo $INPUTFILE0 | awk -F\: '{print $4}'`
  else
    set evtsFile = $INPUTFILE0;
    set f        = 1
    set l        = 200
    set file     = `basename ${evtsFile} .MuDst.root`;
  endif
#  set rung = `echo $file | awk -F\_ '{print $2}'`
  echo "${file}"
  set fileB = `basename ${file} .MuDst.root`;
  set logFile  = ${fileB}B.log
  set sdt = "";
  set Name = `basename $PWD`
#  set sdt = ",sdt20201224"
  set KFVertex = "";
#  echo $Name | grep -i KFV
#  if (! $?) set KFVertex = ",StiKFVertex";
#  set f=`find . -maxdepth 1 -type f -name "*${fileB}*.root"`
#  if ("$f" == "") then
  ls -l *${fileB}*.root Done/*${fileB}*.root
  if (! $?) exit 0;
# root.exe -q -b 'bfc.C(200,"mtin,MC.2020,TpcRS,McTpcAna,StiCA,-geantOut,NoHistos,noRunco","'${file}'",0,"'${fileB}'.root")' &gt;&amp; ${logFile}
#     root.exe -q -b 'bfc.C('${f}','${l}',"mtin,TpcRS,P2020a,TpxRaw,TpxClu,-hitfilt,mtd,btof,BEmcChkStat,CorrX,OSpaceZ2,OGridLeakFull,evout,NoHistos,noTags,noRunco,StiCA,CorrY,McTpcAna,PicoVtxDefault,-in","'${evtsFile}'","'${fileB}'.root")' &gt;&amp; ${logFile}
#     root.exe -q -b 'bfc.C('${f}','${l}',"mtin,TpcRS,pp2009a,TpxRaw,TpxClu,evout,NoHistos,noTags,noRunco,StiCA,McTpcAna,PicoVtxDefault,-in","'${evtsFile}'","'${fileB}'.root")' &gt;&amp; ${logFile}
#     root.exe -q -b 'bfc.C('${f}','${l}',"mtin,MC.2021,TpcRS,McTpcAna,StiCA,-geantOut,phys_off,evout,NoHistos,noTags,noRunco,PicoVtxDefault,-in","'${evtsFile}'","'${fileB}'.root")' &gt;&amp; ${logFile}
     root.exe -q -b 'bfc.C('${f}','${l}',"mtin,MC.2021,TpcRS,McTpcAna,StiCA,-geantOut,phys_off,evout,NoHistos,noTags,noRunco,PicoVtxDefault,-in,UseCAVxFinder","'${evtsFile}'","'${fileB}'.root")' &gt;&amp; ${logFile}
</command>
<!-- <input URL="command:TpcRS_Part.pl" /> -->
<input URL="command:daq2020TpcRS.pl" />
<!-- <input URL="command:TpcRS_Part.pl" />  -->

</job>

