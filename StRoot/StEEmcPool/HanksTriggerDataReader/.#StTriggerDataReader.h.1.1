#ifndef STAR_StTriggerDataReader
#define STAR_StTriggerDataReader
#include "StMaker.h"

class StTriggerDataReader : public StMaker {
public: 
  StTriggerDataReader(const char *name="trgd");
  virtual ~StTriggerDataReader(){};
  virtual Int_t Make();
  virtual Int_t OpenFile(char*);
  virtual Int_t CloseFile();
  void* GetTriggerData() {return mTrg;};

  virtual const char *GetCVS() const
  {static const char cvs[]="Tag $Name:" __DATE__ " " __TIME__ ; return cvs;}
  
protected:
  
private:
  FILE* mFile;
  void* mTrg;

  ClassDef(StTriggerDataReader, 0)   //StAF chain virtual base class for Makers
};
#endif
