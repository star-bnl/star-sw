#include <stdio.h>
#include "dstype.h"

void tbrNewDSTree(DS_DATASET_T **dsPtr,long nDsPtr) {
   printf("tbrNewDSTree can only be used from TAS++\n"); }
void tbrNewTbView(DS_DATASET_T *dsPtr) {
   printf("tbrNewTbView can only be used from TAS++\n"); }
void tbrNewDSView(DS_DATASET_T **dsPtrs,long nDsPtr) {
   printf("tbrNewDSView can only be used from TAS++\n"); }

